node_modules
dist
.DS_Store
.env
.env.local
*.log
.netlify
playwright-report
test-results
blob-report
.playwright

# Regenerated by the demo pipeline: `node demo/capture.mjs`, then
# `node demo/render.mjs`. Kilobytes of code produce megabytes of pictures,
# so only the code is kept.
demo/screens
demo/preview
demo/out
public/demo-stage
